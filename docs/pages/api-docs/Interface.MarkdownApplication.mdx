# MarkdownApplication

The MarkdownApplication extends TypeDoc's Application instance with a custom renderer.

## Extends

- [`Application`](https://typedoc.org/api/classes/Application.html)

## Properties

### renderer

> **renderer**: [`MarkdownRenderer`](/api-docs/Interface.MarkdownRenderer.mdx) & [`Renderer`](https://typedoc.org/api/classes/Renderer.html)

The renderer used to generate the documentation output.

#### Overrides

`Application.renderer`
