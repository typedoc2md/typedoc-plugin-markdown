# MarkdownApplication

This interface is essentially an extended typing of TypeDoc's [`Application`](https://typedoc.org/api/classes/Application.html) instance.``

## Extends

- [`Application`](https://typedoc.org/api/classes/Application.html)

## Properties

### renderer

> **renderer**: [`MarkdownRenderer`](/api-docs/Interface.MarkdownRenderer.mdx)

Re-types the `renderer` instance to [`MarkdownRenderer`](/api-docs/Interface.MarkdownRenderer.mdx).

#### Overrides

`Application.renderer`
