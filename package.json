{
  "name": "typedoc-plugin-markdown",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && npm run copy-templates",
    "copy-templates": "cpx 'src/theme/**/*.hbs' dist/theme",
    "lint": "tslint --type-check --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tgreyuk/typedoc-plugin-markdown.git"
  },
  "keywords": [
    "typedocplugin",
    "typedoc"
  ],
  "author": "Thomas Grey",
  "license": "ISC",
  "dependencies": {
    "typedoc": "0.7.1"
  },
  "devDependencies": {
    "cpx": "1.5.0",
    "tslint": "5.3.2"
  }
}
