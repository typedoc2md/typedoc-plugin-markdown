/**
 * Source of the references re-exported by the locales module.
 *
 * @module
 */

/** An interface re-exported under its own name. */
export interface ReExportedInterface {
  propA: string;
}

/** An interface re-exported under a different name. */
export interface RenamedInterface {
  propB: string;
}
